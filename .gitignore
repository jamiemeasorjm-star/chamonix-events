.env
venv/
__pycache__/
*.pyc
data/*.json

# T10: SQLite canonical storage (runtime state; regenerated by pipeline)
data/*.db
data/*.db-shm
data/*.db-wal

# T19: build artefact snapshots (runtime; regenerated on every build)
data/builds/

# T26: one-off verification artefacts
data/t26_verify*

# Build outputs (regenerated by build.py — do NOT version; this ended up in git
# historically and trips the T33 gate on every build)
index.html
about.html
review.html
submit.html
privacy.html
sitemap.xml
robots.txt
events/
